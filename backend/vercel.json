{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo '' && echo '=========================================================' && echo ' Wrong folder: this is the LEGACY standalone backend.' && echo '' && echo ' The app now ships its API as Next.js Route Handlers, so' && echo ' the whole thing deploys as ONE Vercel project:' && echo '' && echo '   Project Settings -> Root Directory = frontend' && echo '' && echo ' This folder is the old Express + Socket.IO server, kept' && echo ' only for reference/self-hosting. It is a long-running' && echo ' process and cannot run on Vercel Functions.' && echo '=========================================================' && exit 1"
}

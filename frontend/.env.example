# Base URL of the backend API + Socket.IO server.
# Local:      http://localhost:5000
# Production: the deployed backend origin, e.g. https://chatboot-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:5000
